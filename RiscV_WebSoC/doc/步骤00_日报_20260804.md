# 步骤00 — RISC-V Ping 协议栈验证与 TCP 开发启动

**日期**: 2026-08-04

---

## 任务一：FPGA RISC-V Ping 固件调试与验证

### 进展和结果
- 手写 C 代码（ARP + IP + ICMP + 以太网驱动）经过多轮调试后，成功编译并烧录
- **Ping 169.254.1.1 通过**，ARP 请求/应答、ICMP 请求/应答均正常
- LED 流水灯正常运行，确认固件在跑

### 遇到的问题
1. **编译错误**：`ip.c` 调用 `icmp_process(frame, len)` 只传 2 参，函数定义要 3 参；缺少 `ETH_MAX_FRAME_LEN` / `lcpu_local_time_l` 定义
2. **Ping 不通**：FPGA 以太网完全静默，tcpdump 显示 PC 发了 ARP 请求但 FPGA 无应答

### 解决方法/经验总结
1. **编译问题**：补上 `ip_total_len` 提取和 `icmp_len` 计算，`ETH_MAX_FRAME_LEN` 移到 `eth.h`，`lcpu_local_time_l` 用 `rdcycle` 替代软件计数器
2. **Ping 不通（关键踩坑）**：对比原项目工作代码后发现，LCPU 的 RX FIFO 访问有严格规则：
   - **永远不关 REN**（读使能保持为 1）
   - **每帧 POP 两次**（头部弹出包数据，尾部弹出推进到下一包）
   - **直接从 RX/TX FIFO 读写**，不能拷贝到内存缓冲区再处理
   - 初版代码用缓冲区 + `LCPU_RD_STOP()` 关 REN，导致 FIFO 卡死
3. 最终方案：复制原项目 `lcpu_general.h` + `eth.c`/`arp.c`/`ip.c`/`icmp.c`，重写 `main.c` + 最小 `comlib.c`

### 下一步计划
- 继续 TCP 协议栈开发

---

## 任务二：C 代码注释与文档

### 进展和结果
- 全部 6 个 C 源文件加中文注释：文件头功能说明 + 关键流程注解 + 协议数据结构
- 写 README.md（项目结构、编译、烧录、验证步骤、故障排查）
- 代码已推送到 GitHub（`zhu031227/Riscv_WebSoc2`）

---

## 任务三：TCP 协议栈第 1 步 — 发送发动机

### 进展和结果
- 基于原项目 TCP 代码，设计并实现了 `send_raw_frame()` 独立发送函数
- 函数自构造完整的以太网帧：MAC 头 + IP 头（含校验和） + TCP 头（含校验和） + 零填充 → `LCPU_WR_PUSH_PACKET`
- 在 `main.c` 中挂载 `tcp_test_send_syn_ack()` 用于验证

### 遇到的问题
- **TCP SYN+ACK 包未出现在 Wireshark**：启动时独立调用 `send_raw_frame` 发送，但 PC 端抓不到 FPGA 发出的任何 TCP 包
- Ping 正常（ICMP 应答能发出），说明 TX 路径硬件没问题

### 排查方向（进行中）
- 怀疑问题在于 TCP 发送不在收包处理上下文中——ICMP 路径由 `eth_proc()` 先写 MAC 头，TC

P 独立发送跳过了这一环节
- 已修改代码将 TCP 测试挂在 ICMP 回复之后，待烧录验证

### 下一步计划
- 验证挂在 ICMP 路径后的 TCP 发送是否正常
- 若仍不行，排查 TX FIFO 写入时机和 MAC 初始化问题
